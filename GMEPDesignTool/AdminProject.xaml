<UserControl x:Class="GMEPDesignTool.AdminProject"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GMEPDesignTool"
             mc:Ignorable="d" 
             d:DesignHeight="805" d:DesignWidth="797">
    
        <Grid Margin="20,40,10,0" Background="White">

        <Grid.RowDefinitions >
            <RowDefinition Height="*"/>
            <RowDefinition Height="300"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Border Grid.Row="0" BorderBrush="Black" BorderThickness="0.5"  CornerRadius="2">
            <Grid Grid.Row="0" Margin="0,1,1,10" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>

                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Background="LightGray" Margin="0,0,0,10">
                    <Label Content="Project Information" VerticalAlignment="Center" FontSize="16"  FontWeight="Bold"/>
                </Grid>
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0" Content="RFP Email"  Margin="0,0,0,10" Width="auto"/>
                    <Button Grid.Row="0" Content="Upload" Click="UploadRfpEmail_Click" Margin="100,0,0,10" Width="76" HorizontalAlignment="Left" FontSize="16" Height="25"  />
                    <Button Grid.Row="0" Content="View" IsEnabled="{Binding EnableDownloadRfp}" Click="DownloadRfpEmail_Click" Margin="200,0,0,10" Width="76" HorizontalAlignment="Left" FontSize="16" Height="25"  />
                    <Label Grid.Row="1" Content="Project No#"  Margin="0,0,0,10" Width="auto"/>
                    <TextBox Grid.Row="1" Margin="100,0,0,10" IsReadOnly="{Binding ProjectNameReadOnly}" Text="{Binding ProjectNo}" />
                    <Label Grid.Row="2" Content="Name" Margin="0,0,0,10"/>
                    <TextBox Grid.Row="2"  Margin="100,0,0,10" Text="{Binding ProjectName}" />
                    <Label Grid.Row="3" Content="Client" Margin="0,0,0,10"/>
                    <ComboBox Margin="100,0,0,10" Grid.Row="3" ItemsSource="{Binding ClientData}" DisplayMemberPath="Value" SelectedValuePath="Id" SelectedValue="{Binding SelectedClientCompanyId}"/>
                    <Label Grid.Row="4" Content="Architect" Margin="0,0,0,10"/>
                    <ComboBox Margin="100,0,0,10" Grid.Row="4" ItemsSource="{Binding ArchitectData}" DisplayMemberPath="Value" SelectedValuePath="Id" SelectedValue="{Binding SelectedArchitectCompanyId}"/>
                    <Label Grid.Row="5" Content="Street Address" Margin="0,0,0,10"/>
                    <TextBox Grid.Row="5" Margin="100,0,0,10"  Text="{Binding StreetAddress}"/>
                    <Label Grid.Row="6" Content="City" Margin="0,0,0,10"/>
                    <TextBox Grid.Row="6" Margin="100,0,0,10"  Text="{Binding City}" />
                    <Label Grid.Row="7" Content="State" Margin="0,0,0,10"/>
                    <ComboBox Grid.Row="7" Margin="100,0,0,10"
                      ItemsSource="{Binding States}" 
                      SelectedItem="{Binding State}" 
                     />
                    <Label Grid.Row="8" Content="Post Code" Margin="0,0,0,10"/>
                    <TextBox Grid.Row="8" Margin="100,0,0,10"  Text="{Binding PostalCode}"/>
                    <Label Grid.Row="9" Content="Project Path" Margin="0,0,0,10"/>
                    <TextBox Grid.Row="9" Margin="100,0,0,10"  Text="{Binding FileDictionary}"/>
                    <Label Grid.Row="11" Content="Descriptions" Margin="0,0,0,10" />
                    <TextBox Grid.Row="11" Margin="100,0,0,10" Text="{Binding Descriptions}" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" TextWrapping="Wrap" Height="100" />
                    <Button Grid.Row="12" Content="Save" Click="SaveAdminProject" Margin="0,15,0,0" Width="76" HorizontalAlignment="Center" FontSize="16" Height="25"  />
                </Grid>
            </Grid>           
        </Border>
        </ScrollViewer>

        <Border Grid.Row="1" BorderBrush="Black" BorderThickness="0.5" Margin="0,10,0,10" CornerRadius="2">
            <Grid Grid.Row="1"  >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal"  Background="LightGray" Margin="1,1,1,10">
                    <Label Content="Proposals" VerticalAlignment="Center" Width="132" FontSize="16"  FontWeight="Bold"/>
                    <Button IsEnabled="{Binding AddButtonEnabled}" Content="Add" Click="OpenSelectProposalTypeWindow" Width="76" HorizontalAlignment="Center" FontSize="16" Height="25"  Margin="10"/>
                </StackPanel>
                <DataGrid x:Name="MyDataGrid" Grid.Row="1" FontSize="15" Height="230"  ScrollViewer.VerticalScrollBarVisibility="Auto"
          ScrollViewer.HorizontalScrollBarVisibility="Auto"  ItemsSource="{Binding Proposals}" AutoGenerateColumns="False" SelectedItem="{Binding Path=SelectedProposal,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}">
                    <DataGrid.ContextMenu>
                        <ContextMenu Name="ContactContextMenu">
                            <MenuItem Header="Edit" Click="EditProposal_Click"/>
                            <MenuItem Header="Download PDF" Click="DownloadProposal_Click"/>
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                    <DataGrid.Columns>
                        <DataGridTextColumn  Header="Date Created" Width="*" Binding="{Binding DateCreated}" />
                        <DataGridComboBoxColumn Header="Status"  SelectedValueBinding="{Binding StatusId, UpdateSourceTrigger=LostFocus}" SelectedValuePath="Tag">
                            <DataGridComboBoxColumn.ItemsSource>
                                <CompositeCollection>
                                    <ComboBoxItem Content="Pending" Tag="1"/>
                                    <ComboBoxItem Content="Executed" Tag="2"/>
                                    <ComboBoxItem Content="Dead" Tag="3"/>
                                </CompositeCollection>
                            </DataGridComboBoxColumn.ItemsSource>
                        </DataGridComboBoxColumn>
                        <DataGridTextColumn Header="Type" Width="*" Binding="{Binding Type}"/>
                        <DataGridTextColumn Header="Created By" Width="*" Binding="{Binding EmployeeUsername}"/>         

                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>


    </Grid>
</UserControl>