<Window x:Class="GMEPDesignTool.LoadSummary"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GMEPDesignTool"
        mc:Ignorable="d"
        Title="LoadSummary" Height="450" Width="800">
    <Window.Resources>
        <local:IndexConverter x:Key="IndexConverter"/>
    </Window.Resources>
    <Grid Margin="10">

        <Border Background="LightSlateGray" BorderBrush="Black" BorderThickness="2">
            <Grid Background="White" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="5*"/>
                    <RowDefinition Height="3*"/>
                </Grid.RowDefinitions>
                <Border BorderBrush="Black" BorderThickness="1" Grid.Row="0">
                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="40">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="'{0}' Load Summary">
                                <Binding Path="Name"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </Border>
                <DataGrid RowHeight="100" AlternationCount="{Binding components.Count}" HorizontalContentAlignment="Stretch" IsReadOnly="True" HeadersVisibility="None" ScrollViewer.CanContentScroll="False" VerticalAlignment="Top" Grid.Row="1" Background="AliceBlue" AutoGenerateColumns="False" ItemsSource="{Binding components}">
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="{Binding ColorCode}" />
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Width=".1*" Binding="{Binding AlternationIndex, RelativeSource={RelativeSource AncestorType=DataGridRow}, Converter={StaticResource IndexConverter}}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextAlignment" Value="Center" />
                                    <Setter Property="FontSize" Value="40"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Width="*">
                            <DataGridTextColumn.Binding>
                                <MultiBinding StringFormat=" {0} '{1}'">
                                    <Binding Path="ComponentType"/>
                                    <Binding Path="Name"/>
                                </MultiBinding>
                            </DataGridTextColumn.Binding>
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextAlignment" Value="Center" />
                                    <Setter Property="FontSize" Value="40"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Width="*">
                            <DataGridTextColumn.Binding>
                                <MultiBinding StringFormat=" {0} KVA">
                                    <Binding Path="RootKva"/>
                                </MultiBinding>
                            </DataGridTextColumn.Binding>
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextAlignment" Value="Center" />
                                    <Setter Property="FontSize" Value="40"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <Grid Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" MinHeight="100">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border BorderBrush="Black" BorderThickness="1" Grid.Column="0">
                            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="30" Text="TOTAL KVA"/>
                        </Border>
                        <Border BorderBrush="Black" BorderThickness="1" Grid.Column="1">
                            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="30" Text="{Binding RootKva}"/>
                        </Border>
                    </Grid>
                    <Grid Grid.Row="1" MinHeight="100">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border BorderBrush="Black" BorderThickness="1" Grid.Column="0">
                            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="30" Text="TOTAL AMP @"/>
                        </Border>
                        <Border BorderBrush="Black" BorderThickness="1" Grid.Column="1"/>
                    </Grid>
                    <Grid Grid.Row="2" MinHeight="100">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border BorderBrush="Black" BorderThickness="1" Grid.Column="0">
                            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="30" Text="CONCLUSION"/>
                        </Border>
                        <Border BorderBrush="Black" BorderThickness="1" Grid.Column="1"/>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
